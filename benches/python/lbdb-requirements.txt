# Dependencies of the LDBC reference benchmark (in-memory CSR stack), used by
# benches/python/main_lbdb.py + lbdb_algorithms.py.  Install into a dedicated
# venv (the runner creates benches/python/.lbdb-venv on demand):
#
#   uv venv benches/python/.lbdb-venv
#   uv pip install -r benches/python/lbdb-requirements.txt -p benches/python/.lbdb-venv/bin/python
#
icebug==13.0
icebug-format[convert-duckdb]>=1.0.1
pyarrow==25.0.0

# Spark GraphFrames stack, used by benches/python/main_graphframe.py.  The jar
# (io.graphframes:graphframes-spark4_2.13:<version>) is resolved by Spark at
# session creation time via --packages / spark.jars.packages, so only the
# Python-side packages live here.
pyspark==4.1.2
graphframes-py==0.12.1
typing_extensions>=4.11
